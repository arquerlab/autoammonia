[build-system]
requires = ["setuptools>=61.0.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "autoammonia"
version = "0.1.0"
description = "A package for ammonia SDL automation."
authors = [
    { name = "Your Name", email = "your.email@example.com" }
]
readme = "readme.md"
requires-python = ">=3.8"
dynamic = ["dependencies"]

[tool.setuptools]
package-dir = {"" = "src"}

[tool.setuptools.packages.find]
where = ["src"]

[project.scripts]
autoammonia_deploy = "autoammonia.project_deploy:main"
autoammonia_run = "autoammonia.project_trigger:main"
autoammonia_rx_test = "autoammonia.testing.rx_module_test:main"
autoammonia_exp_requester = "autoammonia.experiment_queue:request_experiments"
